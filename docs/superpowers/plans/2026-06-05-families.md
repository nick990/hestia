# Famiglie e movimenti condivisi Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Introdurre famiglie (admin assegna membri), scope `personal`/`family` sui movimenti, viste cashflow Tutti/Famiglia/Solo miei, toggle condiviso nel dialog.

**Architecture:** Migrazione Postgres (`families`, `family_members`, colonne su `movements`, RLS); query cashflow filtrate per `view`; admin CRUD famiglie via service role; UI Settings + segmented control cashflow.

**Tech Stack:** Next.js 16 App Router, Supabase SSR + RLS, shadcn/ui, Vitest, sonner.

**Spec:** [`docs/superpowers/specs/2026-06-05-families-design.md`](../specs/2026-06-05-families-design.md)

---

## File map

| File | Azione |
|------|--------|
| `supabase/migrations/20250605150000_families.sql` | Create |
| `lib/families/types.ts` | Create |
| `lib/families/queries.ts` | Create |
| `lib/cashflow/view.ts` | Create |
| `lib/cashflow/view.test.ts` | Create |
| `lib/cashflow/types.ts` | Modify |
| `lib/cashflow/queries.ts` | Modify |
| `app/actions/families.ts` | Create |
| `app/actions/movements.ts` | Modify |
| `app/(protected)/settings/families/page.tsx` | Create |
| `components/settings/families-manager.tsx` | Create |
| `components/settings/settings-nav.tsx` | Modify |
| `components/cashflow/view-filter.tsx` | Create |
| `components/cashflow/movements-manager.tsx` | Modify |
| `components/cashflow/movements-table-columns.tsx` | Modify |
| `app/(protected)/cashflow/page.tsx` | Modify |
| `docs/MANUAL_TEST.md` | Modify |

---

### Task 1: Migrazione database famiglie e RLS movimenti

**Files:**
- Create: `supabase/migrations/20250605150000_families.sql`

- [ ] **Step 1: Creare migrazione**

```sql
-- Famiglie e scope movimenti personal/family

create table public.families (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  created_at timestamptz not null default now(),
  constraint families_name_not_blank check (trim(name) <> '')
);

create table public.family_members (
  family_id uuid not null references public.families (id) on delete cascade,
  user_id uuid not null references auth.users (id) on delete cascade,
  joined_at timestamptz not null default now(),
  primary key (family_id, user_id),
  constraint family_members_user_unique unique (user_id)
);

create index family_members_family_id_idx on public.family_members (family_id);

alter table public.movements
  add column scope text not null default 'personal',
  add column family_id uuid references public.families (id) on delete restrict;

alter table public.movements
  add constraint movements_scope_check check (scope in ('personal', 'family'));

alter table public.movements
  add constraint movements_scope_family_consistency check (
    (scope = 'personal' and family_id is null)
    or (scope = 'family' and family_id is not null)
  );

update public.movements set scope = 'personal', family_id = null;

create index movements_family_occurred_idx
  on public.movements (family_id, occurred_on desc)
  where scope = 'family';

-- Helper RLS
create or replace function public.current_user_family_id()
returns uuid
language sql
stable
security definer
set search_path = public
as $$
  select family_id from public.family_members where user_id = auth.uid() limit 1;
$$;

revoke all on function public.current_user_family_id() from public;
grant execute on function public.current_user_family_id() to authenticated;

-- families / family_members: read own family; writes via admin client only
alter table public.families enable row level security;
alter table public.family_members enable row level security;

create policy "families_select_member"
  on public.families for select to authenticated
  using (
    id in (
      select family_id from public.family_members where user_id = auth.uid()
    )
  );

create policy "family_members_select_own"
  on public.family_members for select to authenticated
  using (user_id = auth.uid());

create policy "families_no_anon"
  on public.families for all to anon using (false) with check (false);

create policy "family_members_no_anon"
  on public.family_members for all to anon using (false) with check (false);

-- Replace movements policies
drop policy if exists "movements_select_own" on public.movements;
drop policy if exists "movements_insert_own" on public.movements;
drop policy if exists "movements_update_own" on public.movements;
drop policy if exists "movements_delete_own" on public.movements;

create policy "movements_select"
  on public.movements for select to authenticated
  using (
    (scope = 'personal' and user_id = auth.uid())
    or (scope = 'family' and family_id = public.current_user_family_id())
  );

create policy "movements_insert"
  on public.movements for insert to authenticated
  with check (
    user_id = auth.uid()
    and (
      (scope = 'personal' and family_id is null)
      or (scope = 'family' and family_id = public.current_user_family_id())
    )
  );

create policy "movements_update"
  on public.movements for update to authenticated
  using (
    (scope = 'personal' and user_id = auth.uid())
    or (scope = 'family' and family_id = public.current_user_family_id())
  )
  with check (
    user_id = auth.uid()
    and (
      (scope = 'personal' and family_id is null)
      or (scope = 'family' and family_id = public.current_user_family_id())
    )
  );

create policy "movements_delete"
  on public.movements for delete to authenticated
  using (
    (scope = 'personal' and user_id = auth.uid())
    or (scope = 'family' and family_id = public.current_user_family_id())
  );
```

- [ ] **Step 2: Applicare migrazione**

Run: `supabase db push` oppure MCP `apply_migration` su progetto remoto.

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20250605150000_families.sql
git commit -m "feat(db): add families, movement scope and updated RLS"
```

---

### Task 2: Tipi famiglia e parser vista cashflow

**Files:**
- Create: `lib/families/types.ts`
- Create: `lib/families/queries.ts`
- Create: `lib/cashflow/view.ts`
- Create: `lib/cashflow/view.test.ts`

- [ ] **Step 1: Scrivere test parser (falliscono)**

Create `lib/cashflow/view.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { parseCashflowViewParam } from "@/lib/cashflow/view";

describe("parseCashflowViewParam", () => {
  it("defaults to all", () => {
    expect(parseCashflowViewParam(undefined)).toBe("all");
    expect(parseCashflowViewParam("invalid")).toBe("all");
  });

  it("parses valid views", () => {
    expect(parseCashflowViewParam("all")).toBe("all");
    expect(parseCashflowViewParam("family")).toBe("family");
    expect(parseCashflowViewParam("mine")).toBe("mine");
  });
});
```

- [ ] **Step 2: Implementare file**

Create `lib/cashflow/view.ts`:

```ts
export type CashflowView = "all" | "family" | "mine";

const VALID_VIEWS: CashflowView[] = ["all", "family", "mine"];

export function parseCashflowViewParam(value: string | undefined): CashflowView {
  if (value && VALID_VIEWS.includes(value as CashflowView)) {
    return value as CashflowView;
  }
  return "all";
}

export function buildCashflowViewSearchParams(
  params: URLSearchParams,
  view: CashflowView,
): URLSearchParams {
  const next = new URLSearchParams(params);
  if (view === "all") {
    next.delete("view");
  } else {
    next.set("view", view);
  }
  return next;
}
```

Create `lib/families/types.ts`:

```ts
export type Family = {
  id: string;
  name: string;
  created_at: string;
};

export type FamilyMembership = {
  family_id: string;
  family_name: string;
};

export type FamilyWithMembers = Family & {
  members: Array<{
    user_id: string;
    email: string;
    full_name: string | null;
    joined_at: string;
  }>;
};

export type AssignableMember = {
  auth_user_id: string;
  email: string;
  full_name: string | null;
  family_id: string | null;
  family_name: string | null;
};
```

Create `lib/families/queries.ts`:

```ts
import type { FamilyMembership } from "@/lib/families/types";
import { createClient } from "@/lib/supabase/server";

export async function getCurrentUserFamily(): Promise<FamilyMembership | null> {
  const supabase = await createClient();
  const { data, error } = await supabase
    .from("family_members")
    .select("family_id, families(name)")
    .maybeSingle();

  if (error) {
    throw new Error(error.message);
  }

  if (!data) {
    return null;
  }

  const family = data.families as { name: string } | { name: string }[] | null;
  const familyName = Array.isArray(family) ? family[0]?.name : family?.name;

  if (!familyName) {
    return null;
  }

  return { family_id: data.family_id, family_name: familyName };
}
```

- [ ] **Step 3: Run test**

Run: `npm run test -- lib/cashflow/view.test.ts`  
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add lib/families/types.ts lib/families/queries.ts lib/cashflow/view.ts lib/cashflow/view.test.ts
git commit -m "feat: add family types and cashflow view parser"
```

---

### Task 3: Estendere tipi e query movimenti

**Files:**
- Modify: `lib/cashflow/types.ts`
- Modify: `lib/cashflow/queries.ts`

- [ ] **Step 1: Aggiornare `Movement`**

In `lib/cashflow/types.ts` aggiungere:

```ts
export type MovementScope = "personal" | "family";

export type Movement = {
  // ...existing fields...
  scope: MovementScope;
  family_id: string | null;
  author_name: string | null;
};
```

- [ ] **Step 2: Aggiornare query con parametro `view`**

In `lib/cashflow/queries.ts`:

- Import `CashflowView` da `@/lib/cashflow/view`
- Estendere select: `scope, family_id, profiles!movements_user_id_fkey(full_name, email)` (o join equivalente)
- `listMovementsForRange(from, to, view: CashflowView)`:
  - Base: `.gte("occurred_on", from).lte("occurred_on", to)`
  - `view === "family"`: `.eq("scope", "family")`
  - `view === "mine"`: `.eq("scope", "personal")` (RLS già limita a auth.uid())
  - `view === "all"`: nessun filtro scope extra (RLS restituisce personali miei + family)
- `getRangeSummary(from, to, view)` — usa `listMovementsForRange` con stesso `view`
- `mapMovement`: mappare `scope`, `family_id`, `author_name` da profile

- [ ] **Step 3: Verificare build**

Run: `npm run build`  
Expected: errori su call site — risolti in Task 7.

- [ ] **Step 4: Commit**

```bash
git add lib/cashflow/types.ts lib/cashflow/queries.ts
git commit -m "feat(cashflow): add view-scoped movement queries"
```

---

### Task 4: Server Actions movimenti (scope condiviso)

**Files:**
- Modify: `app/actions/movements.ts`

- [ ] **Step 1: Aggiungere `sharedWithFamily` al payload**

In `createMovement` / `updateMovement`:

```ts
import { getCurrentUserFamily } from "@/lib/families/queries";

// input type aggiunge:
sharedWithFamily?: boolean;

// dopo validazione campi:
const membership = await getCurrentUserFamily();
const sharedWithFamily = input.sharedWithFamily === true;

if (sharedWithFamily && !membership) {
  return { ok: false, error: "Non appartieni a una famiglia." };
}

const scope = sharedWithFamily ? "family" : "personal";
const family_id = sharedWithFamily ? membership!.family_id : null;

// insert/update include scope, family_id
```

- [ ] **Step 2: Rimuovere vincolo `user_id` su update/delete**

Sostituire:

```ts
.eq("id", id)
.eq("user_id", user.id);
```

con:

```ts
.eq("id", id);
```

RLS consente update/delete family a qualsiasi membro.

- [ ] **Step 3: Commit**

```bash
git add app/actions/movements.ts
git commit -m "feat(cashflow): support shared family movements in actions"
```

---

### Task 5: Server Actions admin famiglie

**Files:**
- Create: `app/actions/families.ts`

- [ ] **Step 1: Implementare actions**

Pattern identico a `app/actions/members.ts`:

```ts
"use server";

import { requireAdmin } from "@/lib/auth/member";
import { createAdminClient } from "@/lib/supabase/admin";
import { revalidatePath } from "next/cache";

type ActionResult = { ok: true } | { ok: false; error: string };

function revalidateFamilies() {
  revalidatePath("/settings/families");
  revalidatePath("/cashflow");
}

export async function createFamily(name: string): Promise<ActionResult> { /* ... */ }

export async function addFamilyMember(
  familyId: string,
  userId: string,
): Promise<ActionResult> {
  // Verifica user non già in altra famiglia (unique user_id)
}

export async function removeFamilyMember(
  familyId: string,
  userId: string,
): Promise<ActionResult> { /* ... */ }

export async function listFamiliesForAdmin() { /* join members + profiles + members.email */ }

export async function listAssignableMembers() {
  // members con auth_user_id not null + eventuale family corrente
}
```

- [ ] **Step 2: Commit**

```bash
git add app/actions/families.ts
git commit -m "feat: admin server actions for families"
```

---

### Task 6: UI Impostazioni — Famiglie

**Files:**
- Create: `app/(protected)/settings/families/page.tsx`
- Create: `components/settings/families-manager.tsx`
- Modify: `components/settings/settings-nav.tsx`

- [ ] **Step 1: Aggiungere voce nav**

In `settings-nav.tsx`:

```ts
{ href: "/settings/families", label: "Famiglie", adminOnly: true },
```

- [ ] **Step 2: Page server**

`app/(protected)/settings/families/page.tsx`:

- `requireAdmin()` (redirect se non admin)
- Carica `listFamiliesForAdmin()` + `listAssignableMembers()`
- Render `FamiliesManager`

- [ ] **Step 3: `FamiliesManager` client**

Pattern `CategoriesManager` / `MembersManager`:

- Lista famiglie espandibili con membri
- Dialog «Crea famiglia» (nome)
- Select utente registrato + «Aggiungi membro»
- Rimuovi membro (conferma)
- Toast + `router.refresh()`

- [ ] **Step 4: Commit**

```bash
git add app/(protected)/settings/families/page.tsx components/settings/families-manager.tsx components/settings/settings-nav.tsx
git commit -m "feat(settings): admin families management UI"
```

---

### Task 7: Cashflow — vista e toggle condiviso

**Files:**
- Create: `components/cashflow/view-filter.tsx`
- Modify: `app/(protected)/cashflow/page.tsx`
- Modify: `components/cashflow/movements-manager.tsx`
- Modify: `components/cashflow/movements-table-columns.tsx`

- [ ] **Step 1: `ViewFilter` client**

Segmented control (tre `Button` o tab shadcn):

```tsx
type ViewFilterProps = {
  view: CashflowView;
  from: string;
  to: string;
  year: number;
  hasFamily: boolean;
};
```

- Se `!hasFamily`: non renderizzare (utente solo personali)
- Click → `router.push` con `buildCashflowViewSearchParams`, preservando `from`/`to`/`year`

- [ ] **Step 2: Aggiornare `cashflow/page.tsx`**

```tsx
type PageProps = {
  searchParams: Promise<{ from?: string; to?: string; year?: string; view?: string }>;
};

const view = parseCashflowViewParam(params.view);
const family = await getCurrentUserFamily();

const [movements, summary, yearSummary, categories] = await Promise.all([
  listMovementsForRange(from, to, view),
  getRangeSummary(from, to, view),
  getYearMonthlySummaries(year), // invariato — aggregato RLS (tutti i movimenti visibili)
  listCategoryOptions(),
]);

// props: view, hasFamily={family !== null}, familyName={family?.family_name}
```

- [ ] **Step 3: `MovementsManager`**

- Props: `view`, `hasFamily`, opzionale `familyName`
- Render `<ViewFilter />` sopra totali periodo
- Dialog movimento:
  - Stato `sharedWithFamily`, default `true` se `hasFamily`
  - Toggle «Condiviso con la famiglia» visibile solo se `hasFamily`
  - Passa `sharedWithFamily` alle actions
  - In edit: inizializza da `movement.scope === "family"`

- [ ] **Step 4: Colonna «Inserito da»**

In `movements-table-columns.tsx`:

- Aggiungere param `showAuthor: boolean`
- Se `showAuthor` (view `all` o `family`): colonna `author_name ?? email`
- Opzionale vista `all`: badge «Personale» su righe `scope === "personal"`

- [ ] **Step 5: Verificare**

Run: `npm run build && npm run test`

- [ ] **Step 6: Commit**

```bash
git add components/cashflow/view-filter.tsx app/(protected)/cashflow/page.tsx components/cashflow/movements-manager.tsx components/cashflow/movements-table-columns.tsx components/cashflow/movements-table.tsx
git commit -m "feat(cashflow): view filter and shared movement toggle"
```

---

### Task 8: Documentazione test manuali

**Files:**
- Modify: `docs/MANUAL_TEST.md`

- [ ] **Step 1: Aggiungere sezione Famiglie**

```markdown
## Famiglie e movimenti condivisi

- [ ] Admin: Impostazioni → Famiglie → crea famiglia e assegna 2 utenti registrati.
- [ ] Utente A: movimento default condiviso → visibile a B in Tutti e Famiglia.
- [ ] Utente A: movimento personale (toggle off) → visibile ad A in Tutti e Solo miei; B non lo vede.
- [ ] B modifica/elimina movimento family di A → OK.
- [ ] Vista Solo miei: solo movimenti personali propri; nessun family.
- [ ] Totali periodo cambiano tra Tutti / Famiglia / Solo miei.
- [ ] Utente senza famiglia: nessun tab vista; solo personali.
- [ ] Admin rimuove membro: ex-membro non vede più family; movimenti family restano per la famiglia.
```

- [ ] **Step 2: Commit**

```bash
git add docs/MANUAL_TEST.md
git commit -m "docs: add families manual test checklist"
```

---

### Task 9: Verifica finale

- [ ] **Step 1: Test automatici**

Run: `npm run test`  
Expected: PASS

- [ ] **Step 2: Build**

Run: `npm run build`  
Expected: PASS

- [ ] **Step 3: Smoke test manuale**

1. Admin crea famiglia con 2 utenti
2. Entrambi vedono movimento condiviso
3. Personale resta privato
4. Tab vista e totali coerenti

---

## Self-review (copertura spec)

| Requisito | Task |
|-----------|------|
| R1–R2 famiglia / membri | Task 1, 5, 6 |
| R3 scope personal/family | Task 1, 3, 4 |
| R4–R5 RLS personal vs family edit | Task 1, 4 |
| R6 default condiviso | Task 7 |
| R7 vista default Tutti | Task 2, 7 |
| R8 admin assegna | Task 5, 6 |
| R9 senza famiglia | Task 7 |
| R10 totali per vista | Task 3, 7 |
| R11 migrazione personal | Task 1 |
| Solo miei = solo personal | Task 3 (`view=mine`), Task 7 |
| No ÷ N | Non implementato (fuori scope) |
| Colonna Inserito da | Task 7 |
| Year summary | Task 7 (RLS, non filtrato per view — coerente spec) |

Nessun placeholder TBD.

---

## Execution handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-05-families.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — fresh subagent per task, review between tasks

**2. Inline Execution** — execute tasks in this session with checkpoints

**Which approach?**
